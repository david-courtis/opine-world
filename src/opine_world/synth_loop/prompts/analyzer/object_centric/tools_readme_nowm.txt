**game_engine.py** -- NOT YET SYNTHESISED. Synthesis kicks in
  once enough transitions accumulate. Until then, the only
  way to predict next-state is to grep the run log for
  similar (state, action) pairs.
